"use client";
import { Area, AreaChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Lightbulb, Sparkles, TriangleAlert } from "lucide-react";
import { formatMoney } from "@/lib/utils";
const colors: Record<string,string> = { amber: "#f5a524", violet: "#a78bfa", cyan: "#47d7d1", rose: "#fb7185", emerald: "#45d49c", blue: "#60a5fa" };
export function InsightsView({ data }: { data: { insights: { id:string; title:string; body:string; tone:string }[]; spendingByCategory: { name:string; amount:string; percentage:number; accent:string }[]; dailyTrend?: {date:string;amount:string}[] } }) {
  const trend=(data.dailyTrend??[]).map(item=>({date:new Intl.DateTimeFormat("en-IN",{day:"numeric",month:"short"}).format(new Date(`${item.date}T00:00:00`)),amount:Number(item.amount)}));
  return <div className="insights-layout"><section className="panel trend-card"><div className="section-heading"><div><span className="eyebrow">Daily pulse</span><h2>Spending trend</h2></div></div>{trend.length?<div className="trend-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={trend} margin={{top:8,right:6,left:-12,bottom:0}}><defs><linearGradient id="spendFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#f5a524" stopOpacity={.42}/><stop offset="100%" stopColor="#f5a524" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="rgba(255,255,255,.05)" vertical={false}/><XAxis dataKey="date" stroke="#77776f" fontSize={11} tickLine={false} axisLine={false}/><YAxis stroke="#77776f" fontSize={10} tickLine={false} axisLine={false}/><Tooltip contentStyle={{background:"#171715",border:"1px solid #333",borderRadius:12}} formatter={value=>formatMoney(String(value))}/><Area type="monotone" dataKey="amount" stroke="#f5a524" fill="url(#spendFill)" strokeWidth={3}/></AreaChart></ResponsiveContainer></div>:<div className="empty-state compact"><span><Sparkles/></span><h3>No daily trend yet</h3><p>Add expenses to reveal how spending moves through the month.</p></div>}</section><section className="panel chart-card"><div className="section-heading"><div><span className="eyebrow">This month</span><h2>Spending by category</h2></div></div>{data.spendingByCategory.length ? <><div className="donut"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data.spendingByCategory} dataKey="amount" nameKey="name" innerRadius="65%" outerRadius="88%" paddingAngle={3}>{data.spendingByCategory.map(item => <Cell key={item.name} fill={colors[item.accent] ?? colors.amber}/>)}</Pie><Tooltip contentStyle={{ background: "#171715", border: "1px solid #333", borderRadius: 12 }} formatter={value => formatMoney(String(value))}/></PieChart></ResponsiveContainer><div><strong>{data.spendingByCategory.length}</strong><span>active envelopes</span></div></div><div className="chart-legend">{data.spendingByCategory.map(item => <div key={item.name}><span><i style={{ background: colors[item.accent] }}/>{item.name}</span><strong>{formatMoney(item.amount)} <small>{item.percentage}%</small></strong></div>)}</div></> : <div className="empty-state compact"><span><Sparkles/></span><h3>No spending to chart</h3><p>Category insights will appear as you record expenses.</p></div>}</section><section className="signals-column"><div className="section-heading"><div><span className="eyebrow">Signals</span><h2>What deserves attention</h2></div></div><div className="insight-list">{data.insights.map(item => <article className={`insight-card ${item.tone}`} key={item.id}><span>{item.tone === "warning" ? <TriangleAlert/> : <Lightbulb/>}</span><div><h3>{item.title}</h3><p>{item.body}</p></div></article>)}</div>{!data.insights.length && <div className="empty-state compact"><span><Sparkles/></span><h3>Insights will grow with you</h3><p>Record spending to reveal meaningful patterns.</p></div>}</section></div>;
}
