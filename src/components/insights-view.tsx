"use client";
import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from "recharts";
import { Lightbulb, Sparkles, TriangleAlert } from "lucide-react";
import { formatMoney } from "@/lib/utils";
const colors: Record<string,string> = { amber: "#f5a524", violet: "#a78bfa", cyan: "#47d7d1", rose: "#fb7185", emerald: "#45d49c", blue: "#60a5fa" };
export function InsightsView({ data }: { data: { insights: { id:string; title:string; body:string; tone:string }[]; spendingByCategory: { name:string; amount:string; percentage:number; accent:string }[] } }) {
  return <div className="insights-layout"><section className="panel chart-card"><div className="section-heading"><div><span className="eyebrow">This month</span><h2>Spending by category</h2></div></div>{data.spendingByCategory.length ? <><div className="donut"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data.spendingByCategory} dataKey="amount" nameKey="name" innerRadius="65%" outerRadius="88%" paddingAngle={3}>{data.spendingByCategory.map(item => <Cell key={item.name} fill={colors[item.accent] ?? colors.amber}/>)}</Pie><Tooltip contentStyle={{ background: "#171715", border: "1px solid #333", borderRadius: 12 }} formatter={value => formatMoney(String(value))}/></PieChart></ResponsiveContainer><div><strong>{data.spendingByCategory.length}</strong><span>active envelopes</span></div></div><div className="chart-legend">{data.spendingByCategory.map(item => <div key={item.name}><span><i style={{ background: colors[item.accent] }}/>{item.name}</span><strong>{formatMoney(item.amount)} <small>{item.percentage}%</small></strong></div>)}</div></> : <div className="empty-state compact"><span><Sparkles/></span><h3>No spending to chart</h3><p>Category insights will appear as you record expenses.</p></div>}</section><section><div className="section-heading"><div><span className="eyebrow">Signals</span><h2>What deserves attention</h2></div></div><div className="insight-list">{data.insights.map(item => <article className={`insight-card ${item.tone}`} key={item.id}><span>{item.tone === "warning" ? <TriangleAlert/> : <Lightbulb/>}</span><div><h3>{item.title}</h3><p>{item.body}</p></div></article>)}</div>{!data.insights.length && <div className="empty-state compact"><span><Sparkles/></span><h3>Insights will grow with you</h3><p>Record spending to reveal meaningful patterns.</p></div>}</section></div>;
}
